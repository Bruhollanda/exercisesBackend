const resposta = calculaSituacao(4);

expect(resposta).equals('reprovacao');
// Usando a interface expect do chai em nossos exemplos, que significa de fato o que é esperado para determinada variável:
