describe('Quando a média for menor que 7', () => {
  // O describe nos permite adicionar uma descrição para um teste específico ou um grupo de testes.
  // o primeiro parâmetro é a descrição e o segundo uma função para executar o cenário de teste.
  it('retorna "reprovado"', () => {
    /**
     *  Já o it nos permite sinalizar exatamente o cenário que estamos testando naquele ponto.
     * A sintaxe do it é bem semelhante a do describe: ela aceita uma string,
     * qual o comportamento a ser testado, e uma função que executa os testes de fato.
    */ 
  });
});
